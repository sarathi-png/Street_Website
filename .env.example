PORT=3000
SESSION_SECRET=generate-a-random-secret-with-node -e "crypto.randomBytes(32).toString('hex')"
UPLOAD_MAX_SIZE=500
GOOGLE_DRIVE_API_KEY=your-google-drive-api-key

# For Railway — persistent disk mount point (volume)
# UPLOADS_DIR=/data/uploads
# THUMBNAILS_DIR=/data/thumbnails
# DATA_FILE=/data/data.db
